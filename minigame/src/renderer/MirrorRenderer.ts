import type { Mirror } from "../entities/types";

export class MirrorRenderer {
  render(_context: CanvasRenderingContext2D, _mirror: Mirror): void {
    // MVP pass: draw implementation lands with gameplay rendering.
  }
}
