import type { Target } from "../entities/types";

export class TargetRenderer {
  render(_context: CanvasRenderingContext2D, _target: Target): void {
    // MVP pass: draw implementation lands with gameplay rendering.
  }
}
