import type { Prism } from "../entities/types";

export class PrismRenderer {
  render(_context: CanvasRenderingContext2D, _prism: Prism): void {
    // MVP pass: draw implementation lands with gameplay rendering.
  }
}
